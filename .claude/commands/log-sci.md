Invoke the `sci-keeper` agent to log a work session in `sci/worklog.csv`.

The user provided this session info: $ARGUMENTS

If arguments are missing or incomplete, ask only for: phase (conception/phase0/phase1...), activity description, tool used (claude.ai/laptop/ci/autre), and duration in hours if known.

Do not read any file other than `sci/worklog.csv`. Add the new line and confirm with the full line that was added.
